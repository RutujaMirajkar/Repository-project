package interfacetesting;

public interface Test { //interface
	
	void Defie();//incomplete method of interface
	
	//method inside the interface are by default Public and abstract
	
	void Setup();// incomplete method of interface
	
}
